import { ManageDataDirective } from './manage-data.directive';

describe('ManageDataDirective', () => {
    it('should create an instance', () => {
        const directive = new ManageDataDirective();
        expect(directive).toBeTruthy();
    });
});
